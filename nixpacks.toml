[build]
builder = "php"

[phases.setup]
nixPkgs = ["php"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader || true"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "php -S 0.0.0.0:$PORT"